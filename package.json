{
  "name": "eutaktos",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "npm run dev --workspace @eutaktos/web-pwa",
    "build": "npm run build --workspace @eutaktos/web-pwa",
    "typecheck": "npm run typecheck --workspaces --if-present && tsc -p tsconfig.api.json",
    "test": "npm run test --workspaces --if-present && vitest run api && node scripts/check-pwa.mjs && npm run test:adversarial",
    "test:adversarial": "node scripts/run-adversarial-regression.mjs"
  },
  "devDependencies": {
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4",
    "@vitejs/plugin-react": "^6.0.5",
    "typescript": "^7.0.2",
    "vite": "^8.2.1",
    "vitest": "^4.1.10"
  },
  "engines": { "node": ">=24" }
}
